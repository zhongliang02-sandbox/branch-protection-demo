# branch-protection-demo

This repo demonstrates branch protection on `main`.

## Rules

- Direct pushes to `main` are **blocked**
- All changes must go through a pull request
- At least 1 approval required before merging
